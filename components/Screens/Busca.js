import React, { Component } from 'react';
import { Text } from 'react-native';

export default class Busca extends Component<{}> {
    render() {
      return (
        <Text> Pagina de busca. Permitir buscas por textos e por categorias. </Text>
      );
    }
  }